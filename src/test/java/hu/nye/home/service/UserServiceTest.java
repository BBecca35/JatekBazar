package hu.nye.home.service;


public class UserServiceTest{
    /*
    private UserService userService;
    @Mock
    private UserRepository userRepository;

    @Before
    public void init(){
        MockitoAnnotations.initMocks(this);
        userService = new UserService(userRepository);
    }

    @Test
    public void getLoggedUserNameShouldTheProperUserName(){
        String expectedLoggedUserName = "Rebeka";
        when(userRepository.getLoggedUserName()).thenReturn(expectedLoggedUserName);
        String actualUserName = userService.getLoggedUserName();
        Assert.assertEquals(expectedLoggedUserName, actualUserName);
    }
     */

}
